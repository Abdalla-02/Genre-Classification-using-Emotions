# ── Bachelor thesis: film-genre classification from soundtrack emotion ──────
# Python 3.11 or 3.12 (developed and tested on 3.12; see README.md)
# Install into a virtual environment:  pip install -r requirements.txt

# ── Core data science ───────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1

# ── Visualisation ───────────────────────────────────────────────────────────
matplotlib==3.9.0

# ── Audio processing ────────────────────────────────────────────────────────
librosa==0.10.2
soundfile==0.12.1             # librosa I/O backend (not imported directly)

# ── Deep learning (PyTorch only, by design: ONE framework for every model) ───
torch==2.3.1
torchaudio==2.3.1
transformers==4.43.3          # AST and CLAP via HuggingFace
torchvggish==0.2              # VGGish -- the bridge to the Blockbuster dataset
                              # (see sections 10 and 11 of docs/README.md)

# ── Reproducibility / progress reporting ────────────────────────────────────
joblib==1.4.2
tqdm==4.66.4

# ── Interactive exploration only; imported by no experiment script ──────────
# (the experiments are plain .py files under experiments/, so this block can be
#  dropped for a leaner install)
seaborn==0.13.2
jupyterlab==4.2.4
ipykernel==6.29.5

# ── Optional: only if YAMNet via TensorFlow is ever needed ──────────────────
# WARNING: use a separate environment -- see "Dependencies and Notes" in README.md
# tensorflow==2.16.1
# tensorflow-hub==0.16.1
